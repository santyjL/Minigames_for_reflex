import reflex as rx

config = rx.Config(
    app_name="MiniGames",
    backend_host="0.0.0.0",
    backend_port=8000,
    deploy_url="https://minigamesforreflex-production.up.railway.app",  # URL pública de Railway
)
